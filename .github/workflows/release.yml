name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v0.1.0)"
        required: true

permissions:
  contents: write

jobs:
  draft-release:
    name: Draft GitHub Release from tag
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select latest stable Xcode
        # mlx-swift-lm 2.31+ requires Swift 6.1 (Xcode 16+).
        run: |
          if [ -d "/Applications/Xcode_16.2.app" ]; then
            sudo xcode-select -switch /Applications/Xcode_16.2.app
          elif [ -d "/Applications/Xcode_16.1.app" ]; then
            sudo xcode-select -switch /Applications/Xcode_16.1.app
          elif [ -d "/Applications/Xcode_16.0.app" ]; then
            sudo xcode-select -switch /Applications/Xcode_16.0.app
          fi
          swift --version

      - name: Determine tag
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "name=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "name=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          fi

      - name: Verify CHANGELOG entry exists
        run: |
          tag="${{ steps.tag.outputs.name }}"
          version="${tag#v}"
          if ! grep -q "\[$version\]" CHANGELOG.md; then
            echo "❌ CHANGELOG.md does not contain a section for $version"
            echo "   Please update CHANGELOG.md before tagging."
            exit 1
          fi
          echo "✅ CHANGELOG.md has [$version]"

      - name: Run final test before release
        # Same filter as ci.yml: only the truly pure-Swift suites.
        # ProportionalRoPETests touches MLX via MLXArray construction in
        # the rope class init, which crashes `swift test` on the runner.
        run: swift test --filter "ConfigurationTests|PromptFormattingTests"

      - name: Extract release notes from CHANGELOG
        id: notes
        run: |
          tag="${{ steps.tag.outputs.name }}"
          version="${tag#v}"
          awk -v ver="$version" '
            /^## \[/{ if (in_section) exit; if ($0 ~ "\\[" ver "\\]") in_section=1; next }
            in_section { print }
          ' CHANGELOG.md > release_notes.md
          echo "Generated release notes:"
          cat release_notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          name: ${{ steps.tag.outputs.name }}
          body_path: release_notes.md
          draft: true
          prerelease: ${{ contains(steps.tag.outputs.name, '-') }}
